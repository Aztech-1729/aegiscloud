[package]
name = "aegis-plugin-runtime"
version = "1.0.0"
edition = "2021"
description = "Runtime types for Aegis Cloud plugins"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
async-trait = "0.1"
inventory = "0.3"
thiserror = "1.0"
